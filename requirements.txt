# Dependencias de producción
# Usadas por Streamlit Community Cloud y Render.
# En producción los embeddings van via HF Inference API (HF_TOKEN),
# por lo que sentence-transformers/torch NO son necesarios.
#
# Para desarrollo local (sin HF_TOKEN): instala también requirements-dev.txt

# Data & ML
numpy
pandas
scikit-learn
xgboost
joblib

# Vectorstore
faiss-cpu

# LLM, RAG y grafo
langchain
langchain-openai
langchain-community
langgraph
openai

# Embeddings via API (sin torch)
huggingface_hub

# Interfaz
streamlit

# Descarga del vectorstore desde Google Drive
gdown

# Utilidades
python-dotenv
